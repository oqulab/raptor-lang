rootProject.name = "raptor-lang"
dependencyResolutionManagement {
    // Меняем FAIL_ON_PROJECT_REPOS на PREFER_PROJECT
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
    }
}